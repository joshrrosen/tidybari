% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_geography.R
\name{get_geography}
\alias{get_geography}
\title{Get Boston geography boundaries}
\usage{
get_geography(level = c("tract", "block_group", "neighborhood"), year = NULL)
}
\arguments{
\item{level}{Character. Geographic level, one of:
\describe{
\item{"tract"}{Census tracts (default)}
\item{"block_group"}{Census block groups}
\item{"neighborhood"}{BPDA neighborhood districts}
}}

\item{year}{Integer. Optional. Controls which vintage of geometry to use.
Currently only 2010 vintage is available, so this parameter is reserved
for future use.}
}
\value{
An sf object containing polygon geometries with associated attributes.
}
\description{
Returns spatial geometries (sf objects) for Boston at different geographic
levels. These can be used for mapping or joined with ecometric data using
\code{get_ecometrics(geometry = TRUE)}.
}
\examples{
\dontrun{
library(sf)
library(ggplot2)

# Get census tract boundaries
tracts <- get_geography("tract")
plot(tracts["NAME"])

# Get neighborhood boundaries
neighborhoods <- get_geography("neighborhood")

# Map neighborhoods
ggplot(neighborhoods) +
  geom_sf(aes(fill = NAME)) +
  theme_minimal() +
  labs(title = "Boston Neighborhoods")
}
}
